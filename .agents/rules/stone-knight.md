# Stone Knight workspace rule

@../../AGENTS.md

Use the root AGENTS.md as the canonical operating rules and follow its document routing.
The owner must verify this workspace rule is enabled (Always On is recommended for this project).
The main coding agent is the only writer. The `.claude/agents/` files are role definitions for Claude Code, not automatically native Antigravity agents.
Do not claim that permissions, test execution or human approval are guaranteed by this Markdown rule.
